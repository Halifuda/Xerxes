#!/bin/bash

cmake --build build --target Xerxes

bus_list=(1 0)
ratio_list=("0.01" "0.25" "0.5" "0.75" "1")

# bus_list=(1)
# ratio_list=("0.25")

for ratio in ${ratio_list[@]}; do
    for bus in ${bus_list[@]}; do
        build/Xerxes $bus $ratio
    done
done
