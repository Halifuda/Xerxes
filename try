#!/bin/bash

cmake --build build --target Xerxes

topo_list=("chain" "tree" "star" "ring" "full")
memcnt_list=(4 8 16)

for memcnt in ${memcnt_list[@]}; do
    for topo in ${topo_list[@]}; do
        build/Xerxes $topo $memcnt
    done
done
